import { MaintenanceRepository } from '../repositories/maintenance.repository.js';

const buildMaintenancePayload = (maintenance) => {
  return {
    id: maintenance.id,
    // Add other properties as needed
  };
}

export const MaintenanceService = {

};